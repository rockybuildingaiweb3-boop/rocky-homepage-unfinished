import { SiteData } from '../../types';

/**
 * Authoritative global site metadata
 * Replaces legacy network fetch for public/data/data.json
 */
export const CANONICAL_SITE_DATA: SiteData = {
  availablity_date: 'TBD',
};
